<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.payeat">

    <application
        android:name=".Database"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ManagerMenu"></activity>
        <activity android:name=".RestaurantOccupancy" />
        <activity android:name=".ExistOrdersActivity" />
        <activity android:name=".BonAppetitActivity" />
        <activity android:name=".ManagerLoginActivity" />
        <activity android:name=".MenuByTitleActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".ManagerOptions" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>